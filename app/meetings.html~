<template name="meetingTemplates">
  {{> meetings}}
  {{> meetingControls}}
</template>

<template name="meetings">
  {{organizationId}}
  {{#each organizationMeetings}}
    <div style="meeting">
      <a href="#" id="meetingId">{{name}}33</a>
    </div>
  {{/each}}
</template>

<template name="meetingControls">
  <div>
    New Meeting 
    <input type="text" id="newMeetingName" />
    <input type="date" id="newMeetingDate" />
    <input type="time" id="newMeetingTime" />
    <input type="button" id="newMeetingSubmit" value="Submit" />
  </div>
</template>