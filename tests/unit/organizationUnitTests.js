/*
var assert = require('assert');

suite('Organizations', function() {
	test('can be edited by an admin', function(done, server) {
		server.eval(function() {
			organizationId = Organizations.find({}) 
			adminUser = 
			emit('adminCanEdit', Organizations.canEdit(, organizationId));
		});

		server.once('adminCanEdit', function(canEdit) {
			assert.equal(canEdit, true);
		});
	});
});
*/